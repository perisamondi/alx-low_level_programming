#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int  main(void)
{
	int n;
	int a;

	rand(time(0));
        n = rand() - RAND_MAX / 2;
        
        return (0);
