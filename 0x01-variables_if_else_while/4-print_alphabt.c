#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char la;

	la = 'a'
		;

	while
		(la = 'a'; la <= 'z');

	if ((la != 'e' && la != 'q'));
	{
		putchar(la);
		la++;
	}
	putchar('\n');
	return (0);
}
