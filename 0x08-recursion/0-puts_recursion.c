#include  "main.h"

/**
 * printf- prints out a string
 * @s: input
 * @\n: input
 * Return 0 on success
 */
void _puts_recursion(char *s)
{
	printf();
	 char s;
	 int i = 0;
	 char *s = printf(char s[i]);
	 
	 putchar(char *s);
	 putchr('\n');
	 return (0);
}
